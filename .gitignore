node_modules/
dist/
.npm-cache/
*.log

# backend runtime data (regenerated from seed)
server/data.json
server/uploads/

# macOS junk (external drive)
.DS_Store
._*

# full DB backups (contain real user PII) — kept locally only, never committed
backups/

# env (keep .env.example)
.env
.env.local
.env.production
.env.*.local
.dev.vars

# Cloudflare Workers local dev cache/state
.wrangler/

# capacitor build output (native project is committed; copied web assets are not)
ios/App/App/public/

/src/generated/prisma
.vercel
.env*
tsconfig.tsbuildinfo
